== DWM-custom ==

This is a modified version of DWM. Although no attention is payed to
backwards-compatibility towards the main line, patches introduced by
newer revisions are merged into this version.

Applied patches:

    * Xft fonts
    * bstack
    * drawsquare removal
    * grid layout
    * layouts rewrite
    * movestack
    * pertag
    * removal of clickable bar
    * replace of title by clock
    * status on all monitors
    * statuscolors
    * tag client counts
    * tag selection bar
    * tagset rewrite
    * urgentborder
